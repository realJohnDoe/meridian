{
  "name": "meridian-oauth-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "cf-typegen": "wrangler types",
    "predev": "wrangler types",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "pretypecheck": "wrangler types",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "4.113.0"
  }
}
