# Moved to `../reports/storage-backend.md`

This file is a **tombstone**, not a survey. The storage-backend comparison now
lives at [`plans/reports/storage-backend.md`](../reports/storage-backend.md) —
it was renamed in `40af40d9` ("Rename survey files to drop the
`plans/surveys/*-survey` stutter"), and moved from `surveys/` to `reports/`
because it records a completed comparison rather than a procedure to re-run.

It is kept at the old path because the published positioning post
(`blog/1-meridian-why-i-built-a-markdown-first-calendar/`) links here, and that
post is frozen — so the link is repaired at this end instead. Do not delete this
file without first checking the post no longer points at it, and do not treat it
as a survey: `plans/surveys/` otherwise holds only runnable survey procedures.
